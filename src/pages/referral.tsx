import MainPageLayout from 'components/Layouts/MainPage'
import Referral from 'views/Referral'

const ReferralPage = () => {
  return <Referral />
}

ReferralPage.title = 'Refer A Friend'
ReferralPage.Layout = MainPageLayout

export default ReferralPage
